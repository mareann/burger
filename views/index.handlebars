<h1>Hamburger Time!</h1>

<div class="container-fluid">

  <div class="row">
    <div id="makeBurger" class="col-sm-4">

      <h2>Make a Burger!</h2>
      <form class="create-form">
        <div class="form-group">
          <label for="ca">Burger Name:</label>
          <input type="text" id="ca" name="name">
        </div>
        <button type="submit">Make Burger</button>
      </form>
    <div id="readyBurgers">
      <h2>Burgers that are ready!</h2>
      {{!comment here }}
      <ul>
        {{#each burgers}}
          {{#unless this.devoured}}
            {{> burgers/burger-block devoured=false}}
          {{/unless}}
        {{/each}}
      </ul>
    </div>

    </div>
    <div class="col-sm-2">
      <img src="assets/img/chef.png">
    </div>
    <div class="col-sm-1">
      <img id="burger-header-img" class="img-responsive center-block" src="assets/img/burger.png"/>
      <img id="burger-header-img" class="img-responsive center-block" src="assets/img/burger.png"/>
      <img id="burger-header-img" class="img-responsive center-block" src="assets/img/burger.png"/>
      <img id="burger-header-img" class="img-responsive center-block" src="assets/img/burger.png"/>
      <img id="burger-header-img" class="img-responsive center-block" src="assets/img/burger.png"/>
    </div>
    <div id="devourBurger" class="col-sm-4">
      <h2>Burgers that are devoured!</h2>
      <ul id="devourList">
       {{#each burgers}}
          {{#if this.devoured}}
            <li class="devouredBurger">
              {{this.burger_name}}
            </li>
          {{/if}}
        {{/each}}
      </ul>
    </div>

    </div>
  </div>

</div>
